// Export the actual Svelte components
export { default as DocsSidebar } from '../components.svelte';
export { default as DocsContent } from '../DocsContent.svelte';

// Re-export the legacy class-based components and utilities
export * from '../components';